# Tile system understood — lesson 1 complete

User answered all 4 quiz questions correctly on first attempt: tile count formula (4^z), raster vs vector distinction, XYZ coordinate meaning, and knowing to use a GeoJSON/vector source for drawn annotations over satellite raster.

**Implications:** The foundational mental model is solid. Ready to move into MapLibre-specific structure (sources and layers). Can reference the tile system without re-explaining it. Can use "raster source" and "vector source" terminology without defining them.
